# AMMT 프로젝트 실행 가이드

이 문서는 원본 보존·인과 split·weak target 정책을 전제로 한 재현 가능한 실행 순서다. 모든 Python 코드는 사용자가 직접 실행하며, `raw_original/`은 읽기 전용이다.

## 1. 현재 완료된 순서

| 순서 | 코드 | 역할 | 주요 입력 | 주요 출력 | 상태 |
|---:|---|---|---|---|---|
| 1 | `audit_layer_camera.py` | A TIFF 구조·밝기·포화 audit | A TIFF | `processed/audit_after_spreading/` | 완료 |
| 2 | `audit_ab_pairs.py` | A/B 대응성·difference 안전성 확인 | A/B TIFF | `processed/audit_ab_pairs/` | 완료 |
| 3 | `src/audit_roi_saturation.py`, `src/audit_roi_candidates.py` | ROI와 LED/stage 포화 비교 | A/B TIFF | `processed/roi_*` | 완료 |
| 4 | `src/build_causal_manifest.py` | K=4 causal split와 guard band 생성 | layer 설정 | `manifests/causal_sequence_manifest.csv` | 완료 |
| 5 | `src/estimate_train_normalization.py` | train-only p01/p99·포화 통계 | A/B TIFF, manifest | `processed/normalization_v1/` | 완료 |
| 6 | `src/ammt_causal_dataset.py` | intensity 3ch + validity mask 3ch Dataset 검증 | TIFF, manifest, normalization config | 메모리 sample | 완료 |
| 7 | `src/audit_registered_xct_targets.py` | sparse XCT schema·coverage·분포 audit | registered XCT CSV | `processed/xct_target_audit/` | 완료 |
| 8 | calibration audit scripts | machine XY→camera projection 검증 | control points, CSV, TIFF | `processed/calibration/` | 완료 |
| 9 | `src/audit_projected_xct_target_support.py` | XCT sparse support의 camera FOV·collision audit | calibration config, XCT CSV | `processed/projected_xct_support/` | 완료 |
| 10 | `src/audit_weak_target_rasterization.py` | sigma별 continuous response/support rasterization audit | calibration config, XCT CSV | `processed/weak_target_audit/` | 완료 |
| 11 | `src/ammt_weak_target_dataset.py` | causal A/B input에 endpoint-layer sparse XCT continuous response·support mask를 on-the-fly 연결 | TIFF, manifest, normalization·calibration·weak-target config, registered XCT CSV | 메모리 sample: `weak_response`, `weak_support_mask`, `weak_target_available`, `weak_supervised_pixel_count` | 완료·z=4/z=128 user sample 검증 |
| 12 | `src/ammt_masked_regression_loss.py` | `weak_support_mask`가 1인 pixel에서만 Smooth L1 continuous regression 계산 | candidate response prediction, `weak_response`, `weak_support_mask` | differentiable loss, supervised pixel/sample count | 완료·runtime 검증 통과 |
| 13 | `src/verify_masked_regression_loss.py` | early unknown 및 available support sample에서 loss·gradient masking을 read-only 검증 | 6-channel Dataset과 loss config | terminal JSON summary만 출력 | 완료·`overall_pass=true` |
| 14 | `src/train_a_only_baseline.py` | 6-channel A history에서 sigmoid continuous candidate map을 예측하고 masked loss로 학습·평가 | A TIFF, causal/normalization/weak/loss/baseline config, XCT CSV | `outputs/`의 checkpoint·history·test metric·compact candidate JSON | run 1 완료·loss 수렴 확인·plateau-safe decoder 보완 |
| 15 | `src/diagnose_a_only_spatial_predictions.py` | trained checkpoint map의 spatial range/std·target range/std·support correlation·decoder status를 read-only 진단 | run 1 checkpoint, A TIFF, manifest/config/XCT CSV | terminal JSON only | 완료·input-invariant top-score plateau 확인 |
| 16 | `configs/a_only_baseline_e24_v1.yaml` + `src/train_a_only_baseline.py` | run 1과 고정된 조건에서 epoch만 8→24로 늘린 controlled training-duration experiment | A TIFF, 기존 manifest/config/XCT, e24 config | e24 best checkpoint | 완료·best validation e9=0.06152481, duration 가설 보류 |
| 17 | `src/evaluate_a_only_checkpoint.py` | 이미 저장된 e24 checkpoint만 read-only로 test masked loss와 plateau-safe candidate JSON 생성 | e24 checkpoint, test Dataset, loss/config | `test_metrics.json`, `test_coordinate_candidates.json` | 완료·test=0.07370871, 48/48 plateau withheld |
| 18 | `configs/a_only_baseline_c32_v1.yaml` + `src/train_a_only_baseline.py` | run 1과 동일하게 epoch=8을 유지하고 base channel만 8→32로 증대 | A TIFF, 기존 manifest/config/XCT | `outputs/a_only_baseline_c32_v1/`의 checkpoint·metrics·candidate JSON | 구현 완료·사용자 실행 대기 |

## 2. 현재 확정된 입력 정책

`configs/normalization_v1.yaml`은 A/B·LED별 train-only robust normalization을 정의한다. `configs/calibration_v1.yaml`은 provisional rank2 `mirror_rotate_270` camera projection과 `(0,-6)` raw-pixel correction을 정의한다. `configs/weak_target_v1.yaml`은 registered command XY(열 3–4), response `xct_5x5x5`(열 40), train-only p01=0.40070/p99=0.58533 robust scaling, sigma=2 model pixel, unknown support policy를 고정한다. 모델 입력은 `[K,6,H,W]`이며 3 intensity channel과 3 validity-mask channel으로 구성된다. weak target은 direction-unresolved continuous XCT-derived quality response이고 support 밖은 unknown이다.

## 3. Weak target Dataset 연결 및 검증 완료

**코드 위치:** `src/ammt_weak_target_dataset.py`이며 프로젝트 root `~/ammt_project`에서 실행한다. 이 wrapper는 원본 TIFF를 `memmap`으로 읽고, manifest의 endpoint `z`에 해당하는 registered XCT CSV만 읽어 command XY를 raw camera pixel로 투영한다. working ROI와 256×256 model grid에 맞춰 sigma=2 Gaussian weighted response를 메모리에서 생성하고, dense target 파일은 저장하지 않는다.

| 검증 sample | 실행 인덱스·endpoint | 확인 결과 | 의미 |
|---|---|---|---|
| early unavailable | `train --index 0`, z=4 | support fraction=0.0, supervised pixel=0, available=false | early XCT 부재가 normal/zero target이 아니라 **unknown·loss 제외**로 처리됨 |
| available support | `train --index 124`, z=128 | input `[4,6,256,256]`, target `[1,256,256]`, support fraction=0.05247498, supervised pixel=3,439, supported response range `[0.0, 0.85119]`, available=true | command-XY calibrated projection과 train-only robust scaling이 실제 target sample에서 동작함 |

**실행 전 상태:** raw TIFF/CSV는 불변이고 `processed/`에 dense target이 없다. **실행 후 상태:** 메모리 sample dictionary에 다음 항목만 추가된다.

| key | shape·type | 역할 |
|---|---|---|
| `weak_response` | `[1,256,256]`, float | clipped train-p01/p99 scaled continuous XCT-derived response |
| `weak_support_mask` | `[1,256,256]`, float 0/1 | supervised pixel=1, unknown=0 |
| `weak_target_available` | scalar bool | endpoint layer에 하나 이상의 camera-ROI support가 존재하는지 표시 |
| `weak_supervised_pixel_count` | scalar int64 | support-mask weighted loss에 사용할 valid target pixel 수 |

## 4. Support-mask weighted continuous regression loss

**코드 위치:** `src/ammt_masked_regression_loss.py`이며, 학습 코드가 model의 `[batch,1,256,256]` candidate-response prediction을 만들면 이 모듈에 전달한다. `configs/masked_regression_loss_v1.yaml`은 Smooth L1의 `beta=0.1`, global supported-pixel mean reduction, unknown handling을 고정한다.

> **수식:** `L = Σ_i m_i · SmoothL1(p_i, y_i) / Σ_i m_i`이다. `p`는 model의 `[0,1]` continuous response prediction, `y`는 `weak_response`, `m`은 `weak_support_mask`다. `Σ_i m_i=0`인 batch는 loss=0으로 반환한다.

`SmoothL1`은 일반적인 squared error보다 sparse XCT response의 국소적 정합 오차와 outlier에 덜 민감하다. 이 loss는 defect classifier loss가 아니며, response direction이 unresolved인 현 상태에서 “XCT-derived continuous quality response를 잘 재현하는가”만 학습한다.

### Read-only 검증 명령

프로젝트 root에서 아래 명령을 실행한다. 이 검증은 model training이 아니며, TIFF/CSV를 읽기 전용으로 사용하고 terminal JSON 외의 파일을 만들지 않는다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/verify_masked_regression_loss.py \
  --stage A \
  --tiff raw_original/layer_camera/LayerCameraAfterSpreading.tif \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --registered-root raw_original/registered_xct \
  --calibration-config configs/calibration_v1.yaml \
  --weak-target-config configs/weak_target_v1.yaml \
  --loss-config configs/masked_regression_loss_v1.yaml \
  --split train \
  --unavailable-index 0 \
  --available-index 124
```

| 검증 항목 | 통과 조건 | 실제 결과 | 확인하는 위험 |
|---|---|---|---|
| z=4 unknown | `loss=0`, supervised pixel=0, 모든 gradient sum=0 | **통과:** loss=0.0, supported/unsupported gradient sum=0.0 | XCT 미지원 layer를 normal target으로 학습하는 오류 |
| z=128 supported response | positive loss, supported gradient sum>0 | **통과:** 3,439 pixel, baseline loss=0.60062975, supported gradient=0.99932176 | sparse supervised point에서만 regression 신호가 생기는지 |
| unknown prediction 불변성 | unknown pixel prediction을 1000으로 바꿔도 loss difference≤`1e-8` | **통과:** loss difference=0.0 | support 밖 pixel이 학습 loss에 새는 오류 |
| unknown gradient 차단 | unsupported gradient sum=0 | **통과:** unsupported gradient sum=0.0 | target이 없는 camera pixel이 training update를 유발하는 오류 |

## 5. A-only baseline

**코드 위치:** `src/train_a_only_baseline.py`이며 project root `~/ammt_project`에서 실행한다. `configs/a_only_baseline_v1.yaml`은 A-stage, K=4, six-channel input, base channels=8, causal past-padded Conv3D aggregation, sigmoid output, AdamW, 8 epochs를 고정한다. 이 모델은 기존 3-channel skeleton을 그대로 쓰지 않고 **3 intensity+3 validity-mask의 6채널 contract**를 직접 소비한다.

입력은 `[B,4,6,256,256]`이고 output은 `[B,1,256,256]`이다. raw candidate logits에는 sigmoid를 적용해 `[0,1]` continuous response prediction을 만들며, `SupportMaskedSmoothL1Loss`가 `weak_support_mask==1` pixel에서만 regression을 계산한다. causal temporal convolution은 왼쪽(past) 방향에만 padding하므로 future layer를 참조하지 않는다.

### 5.1 Read-only dry-run

dry-run은 무작위 초기화 model이 shape·range·loss contract에 맞게 forward되는지만 확인한다. 학습, optimizer step, checkpoint, heatmap, output file 생성이 전혀 없다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/train_a_only_baseline.py \
  --config configs/a_only_baseline_v1.yaml \
  --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --registered-root raw_original/registered_xct \
  --calibration-config configs/calibration_v1.yaml \
  --weak-target-config configs/weak_target_v1.yaml \
  --loss-config configs/masked_regression_loss_v1.yaml \
  --dry-run \
  --dry-run-index 124
```

| dry-run 확인 항목 | 기대 조건 |
|---|---|
| input shape | `[1,4,6,256,256]` |
| prediction shape | `[1,1,256,256]` |
| prediction range | sigmoid 때문에 0 이상 1 이하 |
| endpoint | index 124이므로 z=128 |
| target support | supervised pixel count=3,439 |
| masked loss | **통과:** 0.11099906. 양수 finite 값이며, 무작위 초기 model의 성능 수치가 아님 |
| device | **통과:** Apple `mps`에서 forward·masked loss 계산 성공 |

### 5.2 실제 baseline 학습

dry-run 통과 후 아래 명령을 실행한다. **실행 전 상태:** raw TIFF/CSV는 읽기 전용이며 `outputs/a_only_baseline_v1/`이 없어야 한다. 기존 output을 덮어쓰지 않도록 directory가 이미 있으면 script가 중단한다. 재실행은 검토 후 새 `--output-dir` 이름을 사용한다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/train_a_only_baseline.py \
  --config configs/a_only_baseline_v1.yaml \
  --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --registered-root raw_original/registered_xct \
  --calibration-config configs/calibration_v1.yaml \
  --weak-target-config configs/weak_target_v1.yaml \
  --loss-config configs/masked_regression_loss_v1.yaml \
  --output-dir outputs/a_only_baseline_v1
```

| 학습 후 생성 파일 | 내용 | Git 정책 |
|---|---|---|
| `best_validation_supported_loss.pt` | validation supported-pixel mean Smooth L1가 가장 낮은 epoch의 small checkpoint | `outputs/` 제외 |
| `training_history.json` | epoch별 train/validation masked loss·support pixel/sample count·optimizer step 수 | `outputs/` 제외 |
| `test_metrics.json` | best checkpoint의 held-out causal test supported-pixel metric | `outputs/` 제외 |
| `test_coordinate_candidates.json` | test endpoint마다 top-5 local-maximum `(x_pixel,y_pixel,layer_z,score)` 후보 | `outputs/` 제외 |

candidate JSON은 dense heatmap을 저장하지 않는다. `score`는 sigmoid-scaled **XCT-derived continuous quality candidate**이며, response direction이 unresolved이므로 anomaly probability, defect class, pass/fail score로 해석하지 않는다.

## 6. 다음 실행 순서

1. **완료:** A-only dry-run에서 input `[1,4,6,256,256]`, prediction `[1,1,256,256]`, z=128 support 3,439 pixel, finite loss=0.11099906, device=`mps`를 확인했다. 무작위 초기 model의 output이 약 0.518로 spatially constant인 것은 아직 학습 전이므로 정상이다.
2. A-only baseline 학습에서 validation supported-pixel mean Smooth L1 checkpoint를 선택한다.
3. **run 1 결과:** epoch 8 best validation masked loss=0.06163210, held-out test loss=0.07344962. Training plumbing과 supported-pixel regression은 동작했지만 test loss는 validation보다 19.17% 높다.
4. **localization hold:** 현재 `test_coordinate_candidates.json`은 48 endpoint×top-5=240개 record 중 score가 한 값(0.67868608)으로, model-space coordinate도 5개 위치가 모든 endpoint에 반복된다. 이는 flat output plateau에서 발생한 임의 top-k tie이므로 실제 품질 후보 위치로 사용하지 않는다.
5. **실행 완료:** test z=203/227/250에서 global map range=0.67868543, std=0.11746240이지만 support 내부 prediction std=0.0·Pearson=null인 고정 plateau를 확인했다. 기존 global range gate는 `emitted`를 반환했으므로 coordinate를 물리 후보로 사용하지 않는다.
6. **실행 완료:** z=203/z=227/z=250 각각에서 top-score tie=63,504 pixel=96.8994%, z=203→227 및 z=227→250 map MAE/max-absolute=0.0을 확인했다. 세 endpoint 모두 `withheld_top_score_plateau`, candidate count=0으로 안전하게 보류됐다.
7. `top_score_tie_fraction > 0.001`이면 `withheld_top_score_plateau`, 연속 endpoint map의 MAE와 max-absolute difference가 모두 `≤1e-6`이면 `withheld_temporally_invariant_map`을 반환한다. 적용 순서는 full-map flat → top-score tie plateau → temporal invariance → local-maximum emission이다. 이 값은 **support mask 없이** model prediction map만 사용한다.
8. **E24 held-out 완료:** best validation epoch=9, masked loss=0.06152481로 run 1보다 0.17% 낮았으나 held-out test loss=0.07370871로 run 1보다 0.35% 높았다. epoch 10–24 validation은 다시 상승/정체했으므로 training duration은 localization 해결 축으로 지지되지 않는다.
9. e24 test candidate loop에 있던 `sample=dataset[index]` 누락은 복구했고, checkpoint-only evaluator가 test JSON을 생성했다. E24는 48/48 endpoint에서 `withheld_top_score_plateau`, candidate=0이었다.
10. E24에서도 top-score tie fraction=96.8994%, selected endpoint map MAE/max-absolute=0/0, support-region prediction std=0으로 run 1과 같은 input-invariant plateau가 유지됐다.
11. C32 run은 epoch=8, model `base_channels=32`로 실행 완료됐다. Run 1과 비교해 top-score tie fraction은 96.8994%→0.3845%로 줄었지만, held-out test loss는 0.24% 높고 selected endpoint map MAE/max-absolute=0/0과 support prediction std=0이 유지됐다.
12. 따라서 capacity는 global static geometry를 바꾸었으나 input-dependent localization을 복원하지 못했다. C32에서도 48/48 endpoint는 `withheld_top_score_plateau`, candidate=0으로 안전하게 보류됐다.
13. 다음 단일 검증은 target rasterization의 Gaussian sigma만 2→3으로 바꾸는 read-only support-density audit이다. raw input/CSV/calibration/response scaling 및 unknown policy를 고정하고 coverage와 blur trade-off를 확인한다.
14. output은 direction 검증 전까지 `XCT-derived quality candidate`로 표현한다.
15. XCT segmentation 또는 수동 검토로 high/low response의 물리적 anomaly direction을 검증한 뒤에만 anomaly likelihood나 binary threshold를 정의한다.

### 6.1 Spatial diagnostic 실행

**코드 위치:** `src/diagnose_a_only_spatial_predictions.py`이며 project root에서 실행한다. checkpoint와 원본 input/target source를 읽기 전용으로 사용해 selected endpoint의 prediction spatial variation, support 내 target variation, Pearson correlation, plateau-safe decoder status를 terminal JSON으로 출력한다. model update, checkpoint 변경, dense heatmap/crop/target/result file 생성은 없다. Decoder 통합 중 발견된 `history` input-assign SyntaxError는 checkpoint/TIFF/XCT 접근 전에 복구됐으며, 재실행으로 import·data-flow를 확인한다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/diagnose_a_only_spatial_predictions.py \
  --config configs/a_only_baseline_v1.yaml \
  --checkpoint outputs/a_only_baseline_v1/best_validation_supported_loss.pt \
  --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --registered-root raw_original/registered_xct \
  --calibration-config configs/calibration_v1.yaml \
  --weak-target-config configs/weak_target_v1.yaml \
  --split test \
  --indices 0 24 47
```

| diagnostic 출력 | 해석 |
|---|---|
| `prediction_statistics.spatial_range`, `std_population` | **관측:** global range=0.67868543, std=0.11746240. 전역 map은 constant가 아님 |
| `supported_prediction_statistics` | **관측:** 세 test endpoint에서 min=max=0.67868608, std=0.0. supervised region은 완전히 평탄 |
| `supported_target_statistics` | **관측:** target std=0.13695–0.15744로 non-flat. target이 아니라 model variation이 병목 |
| `prediction_target_pearson_on_support` | **관측:** `null`. supported prediction variance=0이므로 상관이 정의되지 않음 |
| `flat_by_spatial_range_atol` | **관측:** `false`. global `max-min≤1e-6` rule만으로는 support-region plateau를 잡지 못함 |
| `top_score_tie_pixel_count`, `top_score_tie_fraction` | **관측:** 63,504 pixel, 96.8994%. max-score tie가 map 대부분을 점유하므로 top-score plateau |
| `temporal_map_mae_to_previous`, `temporal_map_max_abs_to_previous` | **관측:** z=203→227, z=227→250 모두 0.0. 선택 endpoint의 prediction map이 정확히 동일 |
| `candidate_decoder_status` | **통과:** 세 endpoint 모두 `withheld_top_score_plateau`, count=0. support mask는 decoder 입력이 아님 |

### 6.2 24-epoch controlled training-duration run

**설정 위치:** `configs/a_only_baseline_e24_v1.yaml`이다. Run 1의 `a_only_baseline_v1.yaml`과 비교해 `training.epochs: 24` 및 `storage.output_directory: outputs/a_only_baseline_e24_v1`만 바뀐다. 따라서 기존 output을 덮어쓰지 않고 under-training만 분리한다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/train_a_only_baseline.py \
  --config configs/a_only_baseline_e24_v1.yaml \
  --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --registered-root raw_original/registered_xct \
  --calibration-config configs/calibration_v1.yaml \
  --weak-target-config configs/weak_target_v1.yaml \
  --loss-config configs/masked_regression_loss_v1.yaml \
  --output-dir outputs/a_only_baseline_e24_v1
```

실행 전에는 raw TIFF/XCT CSV 및 `outputs/a_only_baseline_v1/`이 그대로이고 e24 output directory는 없다. 실행 후에는 e24 directory에 best checkpoint, `training_history.json`, `test_metrics.json`, decoder status를 포함한 compact candidate JSON만 생성된다. dense heatmap/target은 저장하지 않는다. 실제 e24 run에서는 epoch 24까지 학습하고 best checkpoint를 썼지만, test loop NameError로 후자의 두 JSON은 생성되지 않았다. 아래 checkpoint-only recovery를 사용하며 24 epoch를 다시 학습하지 않는다.

| run 1과 비교할 항목 | e24 관측 | 판정 |
|---|---|---|
| best validation masked loss | 0.06152481 vs 0.06163192 | 0.17% 낮았지만 best가 e9에 머묾 |
| held-out test masked loss | 0.07370871 vs 0.07344962 | 0.35% 높아져 개선되지 않음 |
| top-score tie fraction | 96.8994% vs 96.8994% | unique local maximum 미형성 |
| map MAE/max-absolute between endpoints | 0/0 vs 0/0 | input history에 반응하지 않음 |
| decoder status/candidate count | 48/48 withheld, 0 | safety는 통과했으나 localization은 보류 |

따라서 e24 duration experiment는 completed negative result다. 다음 비교는 epoch=8을 되돌리고 model base channel만 8→32로 바꾸는 capacity experiment다.

### 6.3 E24 checkpoint-only held-out evaluation recovery

**코드 위치:** `src/evaluate_a_only_checkpoint.py`이다. 이 코드는 e24의 기존 `best_validation_supported_loss.pt`를 read-only로 로드해 test Dataset을 한 번 평가한다. optimizer·backward·checkpoint write는 없고, `outputs/a_only_baseline_e24_v1/`에 없는 `test_metrics.json` 및 `test_coordinate_candidates.json`만 생성한다. 두 파일이 이미 있으면 overwrite를 거부한다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/evaluate_a_only_checkpoint.py \
  --config configs/a_only_baseline_e24_v1.yaml \
  --checkpoint outputs/a_only_baseline_e24_v1/best_validation_supported_loss.pt \
  --output-dir outputs/a_only_baseline_e24_v1 \
  --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --registered-root raw_original/registered_xct \
  --calibration-config configs/calibration_v1.yaml \
  --weak-target-config configs/weak_target_v1.yaml \
  --loss-config configs/masked_regression_loss_v1.yaml
```

| 검사 | 실행 전 | 실행 후 |
|---|---|---|
| Existing checkpoint | e24 training이 저장한 file | read-only, 그대로 유지 |
| Raw TIFF/XCT CSV | 원본 source | memmap/read-only access만 수행 |
| E24 test metric/candidate JSON | NameError 때문에 부재 | checkpoint evaluation 결과로 처음 생성 |
| Training state | e24 epoch 24 종료 상태 | optimizer update·retraining 없음 |

### 6.4 C32 model-capacity controlled run

**설정 위치:** `configs/a_only_baseline_c32_v1.yaml`이다. Run 1의 `a_only_baseline_v1.yaml`과 비교해 model의 `base_channels: 8→32`, 별도 output directory만 다르다. Training epoch은 8로 되돌리고 all other model/data/loss/optimizer/seed/decoder settings을 고정하여, feature capacity가 spatial collapse의 원인인지 분리한다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/train_a_only_baseline.py \
  --config configs/a_only_baseline_c32_v1.yaml \
  --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --registered-root raw_original/registered_xct \
  --calibration-config configs/calibration_v1.yaml \
  --weak-target-config configs/weak_target_v1.yaml \
  --loss-config configs/masked_regression_loss_v1.yaml \
  --output-dir outputs/a_only_baseline_c32_v1
```

실행 전에는 raw TIFF/XCT CSV, run 1/e24 output과 checkpoint가 모두 그대로이며 C32 output directory는 없다. 실행 후에는 C32 directory에 checkpoint, history, held-out metrics, compact candidate JSON만 생성된다. dense target/heatmap은 저장하지 않는다.

| C32 지표 | 실제 관측 | capacity 가설 판정 |
|---|---:|---|
| Held-out masked loss | 0.07363038 vs run 1 0.07344962 | +0.24%, 개선 없음 |
| Top-score tie fraction | 0.3845% vs 96.8994% | static top geometry는 줄었지만 충분하지 않음 |
| Selected endpoint map MAE/max-absolute | 0 / 0 | A history 변화에 반응하지 않음 |
| Support-region prediction std | 0 | sparse target 내 variation 미학습 |
| Decoder status/candidate count | 48/48 withheld, 0 | safety 통과·localization 보류 |

### 6.5 Weak target support-density rasterization audit: sigma=2 vs 3

**코드 위치:** `src/audit_weak_target_support_density.py`이며 project root에서 실행한다. 이 코드는 TIFF를 열지 않는다. 대신 현재 학습 Dataset과 같은 registered CSV command XY, provisional calibration/offset, working ROI, 256×256 model grid, round-to-pixel rule, train p01/p99 scaling, clipping, Gaussian weighted average를 메모리에서 재구성한다. 바뀌는 유일한 값은 Gaussian sigma=2와 3이다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/audit_weak_target_support_density.py \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --registered-root raw_original/registered_xct \
  --calibration-config configs/calibration_v1.yaml \
  --weak-target-config configs/weak_target_v1.yaml \
  --output-dir processed/weak_target_support_density_v1
```

입력은 causal manifest, ROI가 있는 normalization config, provisional calibration/control-point JSON, weak target scaling config, 그리고 requested endpoint layer의 raw registered XCT CSV다. 어떤 TIFF·CSV·config·checkpoint도 수정하지 않는다. 실행 전에는 `processed/weak_target_support_density_v1/`이 존재하지 않아야 한다. 실행 후에는 다음의 **소형 수치 파일 세 개만** 생성된다.

| 출력 파일 | 내용 | 저장하지 않는 것 |
|---|---|---|
| `per_layer_sigma_metrics.csv` | layer·split·sigma별 support fraction, response coverage, connected-component 수·최대 component share | pixel target array, TIFF crop, heatmap PNG |
| `sigma_comparison_by_layer.csv` | sigma=2→3 support gain, base support retention, common-support response MAE/Pearson, component 변화 | dense response/support mask |
| `support_density_summary.json` | median 지표, provisional gate, training-review recommendation | model checkpoint, optimizer state, candidate 좌표 |

기본 endpoint는 `4, 128, 157, 161, 180, 199, 203, 227, 250`이다. z=4는 early XCT absence가 `unknown`으로 유지되는지 확인하는 negative-control layer이며, 나머지는 train/validation/test에 걸친 available target sample이다. 모든 sigma map은 실행 중 RAM에서만 존재하고 종료 시 사라진다.

| Provisional gate | 통과에 필요한 조건 | 검증 이유 |
|---|---|---|
| Gradient coverage | available layer median support gain ≥25% | support-only loss가 참조하는 pixel을 충분히 늘리는지 확인 |
| 기존 signal 보존 | sigma=2 support retention=100% | sigma 확대로 기존 supervised 영역이 사라지지 않는지 확인 |
| Response stability | common-support response MAE ≤0.05 | 기존 known pixel의 continuous response가 과도하게 바뀌지 않는지 확인 |
| Fragment 유지 | median component-count ratio ≥0.5 | nearby support island가 지나치게 단일 영역으로 합쳐지지 않는지 확인 |
| Giant-component 방지 | median largest-component share growth ≤1.5 | 넓은 connected support가 location distinction을 약화시키는지 확인 |

이 gate는 **sigma=3 training config 검토용 engineering rule**일 뿐, defect 판정·response direction 검증·model localization 성공·deployment readiness를 뜻하지 않는다. 하나라도 실패하면 training config를 만들지 않고 `hold_candidate_sigma`로 보류한다. 통과해도 사용자 확인 후에만 별도 controlled training run을 준비한다.

#### 실행 결과 및 판정

| 지표 | Gate | 결과 | 판정 |
|---|---:|---:|---|
| Available endpoint 수 | — | 8 (`z=4`는 sigma=2/3 모두 unknown) | unknown policy 유지 |
| Median support gain | ≥25% | 29.3990% | 통과 |
| Minimum base support retention | 100% | 100% | 통과 |
| Median common-support response MAE | ≤0.05000 | 0.0538972 | **실패**; threshold보다 7.79% 큼 |
| Median component-count ratio | ≥0.5 | 1.0 | 통과 |
| Median largest-component share growth | ≤1.5 | 1.0 | 통과 |

결론은 `automatic_gate_pass=false`, `hold_candidate_sigma`다. Sigma=3은 layer당 1,041 supervised pixel을 추가하고 support fraction을 약 1.58844 percentage points 높였지만, common support에서도 Gaussian weight mixture가 바뀌어 continuous response가 기준 이상으로 달라졌다. 또한 모든 available layer의 binary support는 sigma 2/3 모두 single component였으므로 component metric만으로는 local merge를 판별할 수 없었다. `weak_target_v1.yaml`의 sigma=2를 유지하며 sigma=3 training config/학습은 시작하지 않는다.

다음에는 target을 변경하지 않는다. 저장 checkpoint가 selected A history의 **frame encoder feature → temporal mixer feature → pre-sigmoid logit → sigmoid map** 중 어느 단계에서 input variation을 잃는지 read-only로 측정하는 input-sensitivity diagnostic을 실행한다.

### 6.6 C32 stagewise input-sensitivity diagnostic

**코드 위치:** `src/diagnose_a_only_input_sensitivity.py`이며 project root에서 실행한다. 이 코드는 target/label diagnostic이 아니라 checkpoint 내부의 A input response 추적용이다. `AMMTCausalStageDataset`만 사용하므로 registered XCT CSV, weak target, support mask, masked loss, candidate decoder에 접근하지 않는다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/diagnose_a_only_input_sensitivity.py \
  --config configs/a_only_baseline_c32_v1.yaml \
  --checkpoint outputs/a_only_baseline_c32_v1/best_validation_supported_loss.pt \
  --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --split test \
  --indices 0 24 47
```

입력은 C32 config/checkpoint, A-stage TIFF, causal manifest, train-only normalization config다. TIFF는 `tifffile.memmap(..., mode="r")`로 필요한 A history만 읽는다. 실행 전후로 raw TIFF/CSV/config/checkpoint가 변하지 않으며, 실행 후에도 output folder, heatmap, target, candidate, model file이 생성되지 않는다. Terminal에는 stage별 scalar statistics와 pairwise distance JSON만 출력된다.

| Stage | 실제 측정 | pairwise max-abs가 `>1e-6`일 때의 의미 |
|---|---|---|
| `input_history` | selected A history의 6-channel tensor | Dataset input이 실제로 다름 |
| `encoded_final_history_frame` | endpoint frame의 frame-encoder feature | encoder가 endpoint frame 차이를 보존 |
| `encoded_history` | 4 frame의 encoder feature sequence | encoder가 temporal 입력 전체를 보존 |
| `temporal_final` | past-only Conv3D aggregation 후 feature | causal temporal module이 variation을 보존 |
| `logits` | decoder pre-sigmoid map | decoder가 feature 차이를 출력 map까지 전달 |
| `score` | sigmoid final map | activation 이후에도 numeric difference 유지 |

모든 pair가 특정 stage에서 `≤1e-6`이면 그 stage가 collapse-origin 후보가 된다. 예를 들어 encoder history는 다르지만 temporal final부터 같으면 temporal aggregation/normalization이 후보이고, logits는 다른데 score만 같으면 sigmoid saturation 후보이다. 이 판정은 **수치적 difference 여부**만 보여 준다. score map이 다르더라도 XCT localization 품질이나 candidate deployment readiness가 자동으로 보장되지는 않는다.

#### 실행 결과 및 판정

| Stage | z203↔227 | z203↔250 | z227↔250 | 해석 |
|---|---:|---:|---:|---|
| `input_history` max-abs | 1.0 | 1.0 | 1.0 | 세 causal A input은 구별됨 |
| `encoded_final_history_frame` max-abs | 2.295962 | 2.033926 | 1.931690 | endpoint frame encoder feature는 구별됨 |
| `encoded_history` max-abs | 2.478056 | 2.153886 | 2.508519 | 4-step encoder feature sequence도 구별됨 |
| `temporal_final` MAE / max-abs | 0 / 0 | 0 / 0 | 0 / 0 | **variation이 이 stage에서 처음 소실** |
| `logits`, `score` MAE / max-abs | 0 / 0 | 0 / 0 | 0 / 0 | temporal collapse가 output까지 전달됨 |

Self-consistency check의 `forward_reconstruction_max_abs=0`으로 exposed diagnostic path가 ordinary `model.forward()` output과 일치했다. 따라서 C32 current checkpoint에 대해 frame encoder와 sigmoid가 아니라 `Conv3D → GroupNorm → SiLU` temporal path가 selected input variation을 잃는 직접 관찰 근거가 확보됐다. 이 결과는 causal temporal module 전체가 일반적으로 잘못됐다는 선언이 아니라, **이 architecture/checkpoint/input에서만** 다음 변경 대상을 temporal path로 좁힌다.

다음 single controlled experiment은 optional endpoint-feature residual bypass다. Concept은 `temporal_final = encoded_endpoint + temporal_update`이며 current endpoint A feature와 past-only temporal update만 사용하므로 future layer leakage가 없다. 기존 C8/E24/C32 config에는 flag가 없으므로 default `false`로 former architecture와 checkpoint state-dict compatibility를 보존한다.

### 6.7 C32 temporal-residual controlled dry run

**코드 위치:** existing `src/train_a_only_baseline.py`에 default-off `model.use_endpoint_feature_residual` option을 추가했고 residual-only config는 `configs/a_only_baseline_c32_temporal_residual_v1.yaml`이다. Flag=true일 때만 endpoint encoder feature와 unchanged temporal update를 decoder 전에 합산한다. Training/evaluation/spatial diagnostic/input-sensitivity diagnostic 모두 config의 optional flag를 전달하므로 old config는 false, residual config는 true로 consistent하게 복원된다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/train_a_only_baseline.py \
  --config configs/a_only_baseline_c32_temporal_residual_v1.yaml \
  --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --registered-root raw_original/registered_xct \
  --calibration-config configs/calibration_v1.yaml \
  --weak-target-config configs/weak_target_v1.yaml \
  --loss-config configs/masked_regression_loss_v1.yaml \
  --dry-run \
  --dry-run-index 124
```

| 항목 | 실행 전 상태 | dry run 후 기대 상태 |
|---|---|---|
| Raw A TIFF·registered XCT CSV | immutable/read-only | 그대로; Dataset이 필요한 frame/CSV만 읽음 |
| Residual output directory | 생성하지 않음 | 여전히 생성하지 않음 |
| Model parameter | random initialisation을 RAM에서만 수행 | optimizer/training/checkpoint save 없음 |
| Dense target/heatmap | 없음 | 생성하지 않음 |
| Terminal output | 없음 | `[1,4,6,256,256]` input, `[1,1,256,256]` prediction, z=128 support=3,439, finite masked loss 출력 |

통과 조건은 MPS/CPU device에서 expected input/prediction shape가 나오고, z=128 `weak_target_available=true`, supervised pixel=3,439, finite loss가 출력되며 error가 없는 것이다. Initial random residual prediction의 loss나 score range는 C32 trained result와 비교하지 않는다. 이 run은 input/target/loss/forward wiring만 확인할 뿐 training 또는 localization 평가가 아니다. 통과 결과가 나온 뒤에만 8-epoch residual controlled training의 실행을 별도 요청한다.

#### 실행 결과

Residual dry run은 MPS에서 정상 완료됐다. Input `[1,4,6,256,256]`, prediction `[1,1,256,256]`, endpoint z=128, `weak_target_available=true`, supervised pixel=3,439, finite masked loss=0.18406811을 확인했다. Initial prediction range는 0.12510997–0.68294573이었으며, output directory/checkpoint/dense heatmap이 만들어지지 않았다. 따라서 C32 temporal-residual full training의 **runtime precondition은 통과**다. 이 loss/range는 random initialization one-sample result이므로 C32 held-out test metric 또는 localization quality와 비교하지 않는다.

#### 8-epoch controlled training 및 held-out validation 결과

C32 temporal-residual training은 epoch 8까지 완료됐고 best validation checkpoint는 epoch 6이다. C32 temporal-only와 동일한 172,834 held-out supported pixels/48 samples에서 validation loss는 0.06151949→0.05642983(−8.27%), test loss는 0.07363038→0.06992126(−5.04%)로 낮아졌다. 모든 48 endpoint가 emitted되었고 compact candidate는 총 240개(5개×48)다. 이 count는 defect count가 아니다.

| 검증 | C32 temporal-only | Residual checkpoint | 판정 |
|---|---|---|---|
| Support prediction std | 0 | 0.043991 / 0.041168 / 0.045247 | selected support variation 회복 |
| Top-score tie | 0.3845% | 1/65,536=0.001526% | 0.1% decoder gate 통과 |
| Map difference | z203/227/250 모두 0 | MAE=0.009254–0.011778, max-abs=0.164584–0.225400 | input-dependent map 변화 |
| Stagewise temporal final | 모든 pair 0 | MAE=0.047502–0.051435, max-abs=1.584772–1.799283 | C32 temporal collapse 해소 |
| Score stage | 모든 pair 0 | MAE=0.009254–0.011778, max-abs=0.164584–0.225400 | sigmoid 이후에도 variation 보존 |

`forward_reconstruction_max_abs=0`으로 stagewise diagnostic path는 ordinary forward output과 일치한다. 따라서 residual bypass가 selected held-out histories에서 temporal stage의 input variation을 decoder/score까지 전달하는 것을 확인했다. 그러나 calibration은 provisional이고 response direction이 unresolved이며 sparse support 밖에는 ground truth가 없다. 현 candidates는 계속 XCT-derived continuous quality candidates이며, physical defect/absolute machine coordinate로 해석하기 전 camera-to-command inverse transform의 round-trip·FOV/edge safety를 별도 audit해야 한다.

### 6.8 Calibration-aware compact candidate coordinate audit

**코드 위치:** `src/audit_a_only_candidate_coordinates.py`이며 project root에서 실행한다. Model inference/training을 하지 않는다. Residual run의 already-emitted `test_coordinate_candidates.json`, residual config, normalization ROI, calibration config, existing 16 control points만 읽는다. TIFF, registered XCT CSV, support mask, weak target, model, checkpoint에는 접근하지 않는다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/audit_a_only_candidate_coordinates.py \
  --candidate-json outputs/a_only_baseline_c32_temporal_residual_v1/test_coordinate_candidates.json \
  --model-config configs/a_only_baseline_c32_temporal_residual_v1.yaml \
  --normalization-config configs/normalization_v1.yaml \
  --calibration-config configs/calibration_v1.yaml \
  --output-dir processed/candidate_coordinate_audit_v1
```

| 항목 | 실행 전 | 실행 후 |
|---|---|---|
| 입력 candidate | residual run compact JSON 240 entries | 원본 JSON은 read-only로 유지 |
| Raw data/model | TIFF/XCT CSV/checkpoint/target | 읽거나 변경하지 않음 |
| In-memory calculation | model-grid→raw camera→inverse machine→raw/model | round-trip residual, part/FOV/edge/duplicate checks 계산 |
| Persisted output | `processed/candidate_coordinate_audit_v1/` 없음 | compact candidate CSV, cross-layer repeat CSV, summary JSON만 생성 |
| Dense data | 없음 | dense map/crop/heatmap 없음 |

`coordinate_consistency_pass`는 score domain, 256-grid/ROI/sensor domain, inverse solvability, raw/model round-trip, same-endpoint duplicate absence를 동시에 요구한다. `operational_geometry_pass_under_provisional_calibration`은 추가로 inverse machine coordinate가 known part rectangle 안에 있어야 한다. `edge_safety_pass`는 model grid edge margin 3px를 별도 warning으로 검사한다. Cross-layer repeated model cell은 location persistence observation이며 same-endpoint duplicate failure와 다르다.

Round-trip tolerance pass는 configured rank-2 `mirror_rotate_270` homography와 `[0,-6]` raw offset의 **내부 계산 일관성**만 보여 준다. Fit RMSE=5.212px 및 LOO RMSE=7.028px인 provisional calibration을 independent metrology 수준으로 승격하거나 candidate를 confirmed defect로 바꾸지 않는다.

#### 실행 결과 및 hold decision

| Check group | Actual result | Operational interpretation |
|---|---|---|
| Candidate domain | 240/240 score finite `[0,1]`, model grid/ROI/sensor inside | pass |
| Transform arithmetic | raw-center mapping max=0px, raw round-trip max=`4.69e-13`px, model round-trip max=0px | pass under configured transform |
| Decoder coordinate behavior | same-endpoint duplicate=0, edge margin min=3px, all edge-safe | pass; no boundary warning |
| Cross-layer repetition | 21 model cells repeated across layers | persistence observation, not a failure |
| Inverse machine part containment | 0/240 inside known part rectangles | **operational geometry hold** |

Summary는 `coordinate_consistency_pass=true`, `edge_safety_pass=true`, `operational_geometry_pass_under_provisional_calibration=false`다. 따라서 emitted candidates의 raw pixel fields와 configured transform arithmetic은 internally consistent하지만, current rank-2 calibration/part geometry convention에서는 all candidates가 part exterior로 inverse-project된다. Candidate output을 machine/part physical coordinate로 operationally interpret하거나 confirmed defect로 보고하지 않는다.

다음 candidate-output-only safety improvement인 optional geometry-aware gate를 구현했다. Configured provisional part geometry 안쪽에 inverse-project되는 local maxima만 NMS candidate ranking 대상으로 하고, none이면 `withheld_outside_provisional_part_geometry`를 반환한다. 이 gate는 score map/model parameters를 바꾸지 않으며, provisional calibration limitation을 output metadata에 남긴다. Existing config에서는 default-disabled다.

### 6.9 Residual checkpoint geometry-gated candidate evaluation

**코드 위치:** decoder logic은 `src/train_a_only_baseline.py`, checkpoint-only read-only evaluation은 `src/evaluate_a_only_checkpoint.py`, 전용 config는 `configs/a_only_baseline_c32_temporal_residual_geometry_gate_v1.yaml`이다. 기존 residual checkpoint를 new model로 load하지만 model architecture flag는 same residual=true이고 optimizer/training loop은 시작하지 않는다.

```bash
cd ~/ammt_project
mkdir -p outputs/a_only_baseline_c32_temporal_residual_geometry_gate_v1
/usr/local/bin/python3 src/evaluate_a_only_checkpoint.py \
  --config configs/a_only_baseline_c32_temporal_residual_geometry_gate_v1.yaml \
  --checkpoint outputs/a_only_baseline_c32_temporal_residual_v1/best_validation_supported_loss.pt \
  --output-dir outputs/a_only_baseline_c32_temporal_residual_geometry_gate_v1 \
  --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --registered-root raw_original/registered_xct \
  --calibration-config configs/calibration_v1.yaml \
  --weak-target-config configs/weak_target_v1.yaml \
  --loss-config configs/masked_regression_loss_v1.yaml
```

| 항목 | Before | After |
|---|---|---|
| Input checkpoint | residual epoch-6 `best_validation_supported_loss.pt` | read-only; overwrite 없음 |
| Raw TIFF/registered XCT CSV | original data | read-only Dataset evaluation only |
| Score map/loss | saved model semantics | unchanged; test loss must match checkpoint evaluation up to deterministic numeric behavior |
| Decoder | existing plateau/tie/temporal gate | those gates first, then provisional in-part local-maxima filter |
| Output directory | new, empty | compact `test_metrics.json`, `test_coordinate_candidates.json` only |
| Training artifacts | optimizer/history/new checkpoint | 생성하지 않음 |

Output candidate status must be read with geometry metadata. `withheld_outside_provisional_part_geometry` means no **local maximum** remained inside current provisional known rectangles; `emitted` means in-geometry local maxima were found but not that physical localization is proven. In either status, XCT support is not consulted by the decoder and score remains unresolved-direction XCT-derived continuous quality candidate.

#### Geometry-gated evaluation and candidate audit result

Checkpoint-only evaluation returned epoch=6, optimizer steps=0, held-out loss=0.0699212649, 172,834 supervised pixels/48 samples. This reproduces the original residual checkpoint test loss, so the gate did not alter the saved model, score map, or loss calculation. The decoder reported 48/48 `emitted`, candidate count=240.

A second compact coordinate audit of `outputs/a_only_baseline_c32_temporal_residual_geometry_gate_v1/test_coordinate_candidates.json` completed the safety validation. `coordinate_consistency_pass=true`, `operational_geometry_pass_under_provisional_calibration=true`, and `edge_safety_pass=true`. All 240 candidate inverse coordinates fall inside the configured rectangles: part01=84, part02=126, part03=27, part04=3; none are outside. Raw-camera round-trip error max=`6.43e-13`px, grid round-trip max=0px, same-endpoint duplicate=0, and edge margin min=52px.

This means the new **candidate decoding pathway** is internally consistent under the configured provisional geometry. It does not establish absolute metrology accuracy, physical defect evidence, response direction, or true candidate persistence.

### 6.10 Existing calibration rank 1 vs selected rank 2 sensitivity audit

**코드 위치:** `src/audit_candidate_calibration_rank_sensitivity.py`. Geometry-gated compact candidate JSON, calibration config, existing 16 control points만 read-only로 사용한다. Rank 1/2 homography는 current control points에서 기존 ranking logic으로 재계산하지만, 새 control fitting, rank re-selection, calibration config 수정은 하지 않는다. TIFF/XCT CSV/target/model/checkpoint/score map은 읽지 않는다.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/audit_candidate_calibration_rank_sensitivity.py \
  --candidate-json outputs/a_only_baseline_c32_temporal_residual_geometry_gate_v1/test_coordinate_candidates.json \
  --calibration-config configs/calibration_v1.yaml \
  --compare-ranks 1 2 \
  --output-dir processed/candidate_calibration_rank_sensitivity_v1
```

| 항목 | 실행 전 | 실행 후 |
|---|---|---|
| Candidate input | 240 geometry-gated compact entries | read-only 유지 |
| Calibration input | existing 16 controls, rank 1/2 candidates | rank selection/config unchanged |
| In-memory calculation | same raw candidate를 two inverse homography로 투영 | part containment, same-part agreement, XY shift 계산 |
| Persisted output | output directory 없음 | candidate-level CSV, endpoint-level CSV, summary JSON only |
| Prohibited data/action | TIFF/XCT/target/model/checkpoint/score map, training | 모두 사용·변경하지 않음 |

`rank_2_is_not_reselected`은 rank 2가 계속 configured provisional transform임을 명시한다. `same_containing_part_fraction`과 `machine_coordinate_shift_alternative_to_selected`은 sensitivity measurement이며 physical-error threshold가 아니다. Rank 1에서 candidate의 part membership이 바뀌거나 XY shift가 크면 coordinate reporting은 raw-camera pixel을 primary coordinate로 하고 selected-rank machine coordinate는 provisional metadata로만 제공한다. 결과가 stable해도 independent metrology validation을 대체하지 않는다.

#### Rank-sensitivity result and reporting policy

Rank 1 and rank 2 both put all 240 candidates within some configured part rectangle, but `same_containing_part_fraction=0.0` (0/240). Every endpoint has same-part fraction 0.0; ambiguity is systematic rather than a small boundary effect. Machine-coordinate shift from rank 1 to rank 2 is min=4.168, median=14.463, p95=33.857, max=38.053. Rank 1/2 fit RMSE=5.212058666px and LOO RMSE=7.028278322px are equal at displayed precision, but their orientation and screen-to-part mappings are reversed.

Therefore the active reporting policy is:

| Candidate field | Reporting rule |
|---|---|
| `x_pixel`, `y_pixel` | primary raw layer-camera coordinate |
| `layer_z`, `score` | report with existing causal and unresolved-response semantics |
| `x_model_pixel`, `y_model_pixel` | retain model-grid provenance |
| machine XY | optional `provisional_machine_xy_rank2`; cannot drive machine action or absolute-location claim |
| part identity | optional `provisional_part_rank2`; do not present as factual physical part ID |
| sensitivity evidence | attach rank1-vs-rank2 0% same-part agreement and XY shift summary whenever machine metadata is exported |

### 6.11 NIST authoritative build-layout anchor research

NIST PDR confirms the Overhang Part X4 archive includes Build Metadata, Layer Camera Metadata, Part Geometry, and XYPT command artifacts.[1] The official dataset description identifies Fig. 2 as Part 1–4 placement/numbering at layer 125; it states that the horizontal cylindrical cavity is on the `-X` left side and the overhang feature on the `+X` right side. It also lists the direct layer-camera calibration artifacts `DotGrid_2000x2000.tif`, `SecondaryCamera_Laser00.tif`, and `Checkerboard_2000x2000.tif` in `Layer Camera Metadata.zip`.[2]

| Research outcome | Current project impact |
|---|---|
| Official part layout/asymmetric feature source found | gives a credible visual criterion to distinguish rank 1 vs rank 2 orientation |
| Layer-camera calibration artifact names confirmed | establishes the metrology data needed for a future direct registration audit |
| Metadata archive absent from local project | current calibration config remains unchanged |
| Direct PDR archive retrieval timed out in sandbox | no partial archive is used; no raw/project file is created |

The paper provides a machine-coordinate layout anchor but not an importable raw-camera-pixel homography.

### 6.12 Layer-125 deterministic B-stage orientation overlay audit

**코드 위치:** `src/audit_layer125_orientation_overlay.py`. The script renders deterministic numeric overlays—not generative images—because raw camera coordinates, XYPT positions, and rank transforms must retain exact geometry. It reads a single B-stage layer-125 LED-3 frame through `tifffile.memmap(..., series=0, mode='r')`, reads only laser-on (`power>0`) XYPT positions, and projects the existing rank 1 / configured rank 2 homographies.

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/audit_layer125_orientation_overlay.py \
  --tiff-b raw_original/layer_camera/LayerCameraBurned.tif \
  --xypt "raw_original/process/XYPT Commands/XYPT_L0125.csv" \
  --calibration-config configs/calibration_v1.yaml \
  --layer-z 125 \
  --led 3 \
  --compare-ranks 1 2 \
  --output-dir processed/calibration/layer125_orientation_overlay_v1
```

| Input/output | Contract |
|---|---|
| Raw camera input | exactly B-stage LED 3, layer 125; read-only memmap |
| Command input | layer-125 XYPT finite laser-on positions only; read-only |
| Transforms | existing rank 1 and rank 2 from same 16 controls; no refit/reselection |
| Display | non-saturated p01/p99 normalized only in RAM; original 2000×2000 coordinate axes retained |
| Persisted results | rank1 and rank2 compact overlay PNGs, projection summary CSV, visual-review JSON |
| Never created | dense target/support map, model heatmap, training artifact, calibration config revision |

#### Visual pass/hold review

Open both generated overlay PNGs and compare the projected colored laser-on paths with visible B-stage part shapes. NIST Fig. 2 says that layer 125 has cavity on the `-X` left and overhang on the `+X` right, and it fixes Part 1→Part 4 layout.[1] A visually more coherent rank is **evidence only**, not an automatic calibration decision. If neither overlay clearly aligns, retain rank-2 provisional status and camera-primary coordinate reporting. Even if one overlay is clearly preferred, record a `visual_preference_hypothesis` only; do not edit `calibration_v1.yaml` until a separate review approves a calibration change.

[1] [NIST PDR: Overhang Part X4](https://data.nist.gov/od/id/mds2-2233)
[2] [Lane & Yeung (2020), J. Res. NIST 125:125027](https://doi.org/10.6028/jres.125.027)

## 7. 실행 원칙

* `processed/`는 재생성 가능한 결과이며 기본적으로 Git 추적 대상이 아니다. 다만 검토 근거를 보존하기 위해 11개 QC PNG 사본과 설명은 `docs/qc_images/`, `docs/quality-control-images.md`에 Git 추적한다.
* full-scale saturation `raw==65535`은 validity mask=0이다.
* target support 밖과 early XCT 미지원 layer는 negative/normal이 아니라 unknown이다.
* validation/test는 normalization·target scaling 통계에 사용하지 않는다.
* 새 코드가 추가되면 이 표에 실행 위치, 역할, 입력, 출력, 의존성을 추가한다.


### 6.13 Layer-125 orientation overlay 실행 결과 — visual hold

The user executed the orientation overlay audit successfully. It read only B-stage `[LED=3,z=125]` using memmap, read local `XYPT_L0125.csv`, and created two compact QC PNGs plus summaries under `processed/calibration/layer125_orientation_overlay_v1/`. Its display normalization was non-saturated p01/p99=`8001/64399`, saturated fraction=`0.0327045`, and display stride=2. Neither raw data nor `calibration_v1.yaml` changed.

| Check | Rank 1 (`mirror_rotate_90`) | Rank 2 (`mirror_rotate_270`) | Decision |
|---|---:|---:|---|
| Fit RMSE / LOO RMSE | 5.212059 / 7.028278 px | 5.212059 / 7.028278 px | numerical tie |
| Laser-on rows per part | 37,868 | 37,868 | same source data |
| Projectable / in-sensor fraction | 100% / 100% | 100% / 100% | both feasible |
| Visible projected regions | same four raw-camera regions | same four raw-camera regions | only part identity/order changes |
| Fig. 2 cavity/overhang correspondence | not unambiguous at full-frame B display | not unambiguous at full-frame B display | **hold** |

The rank1 PNG labels the four compact raw-camera path regions from top to bottom as `part01→part04`; rank2 labels the same regions `part04→part01`. The full B-stage frame has diffuse/noisy texture and dense command-path overlay, so it does not supply a defensible measurement of the NIST cavity-left/overhang-right asymmetry. Set `visual_preference_hypothesis=inconclusive`; retain rank 2 with `status: provisional` and camera-primary candidate reporting. Do not use these PNGs to change calibration rank. The next independent anchor is NIST Layer Camera Metadata or a manually verified fiducial, not further model training.


### 6.14 Existing Layer Camera Metadata artifact inventory — archive re-download cancelled

The user identified existing extracted metadata under `raw_original/metadata/Layer Camera Metadata/`. Read-only inventory confirmed the three official independent calibration artifacts, so no duplicate `Layer Camera Metadata.zip` download is needed.

| Local original artifact | Size bytes | Local SHA-256 |
|---|---:|---|
| `DotGrid_2000x2000.tif` | 4,000,384 | `d3b8262b68f47d0c2c010373071b165fc272b87e8d293e0d18e4f54f7f23a331` |
| `SecondaryCamera_Laser00.tif` | 36,869,338 | `fbd520eed70de2888c84037ed24106150acc9361d1ff7e4ab7b5c96bd9e49833` |
| `Checkerboard_2000x2000.tif` | 4,000,148 | `40a77c6e4700434187b4d297b7f2b41372c4b1a4a6c99d4ee1736ab8193e1649` |

All three files have TIFF signatures. The source archive is not kept locally, so these SHA-256 values are local reproducibility fingerprints rather than a comparison to the published archive SHA-256. Preserve all files immutable. The next separately approved stage must inspect these TIFFs read-only and report dot-grid/laser-origin/checkerboard geometry before considering any modification to `calibration_v1.yaml`.


### 6.15 Independent metrology metadata pre-audit — implementation ready

This pre-audit is the first independent calibration stage after the rank ambiguity hold. It uses the existing immutable files in `raw_original/metadata/Layer Camera Metadata/`, not a new download and not candidate/XCT information. Run from the project root only after confirming the output directory does not already exist:

```bash
cd ~/ammt_project
/usr/local/bin/python3 src/audit_independent_metrology_metadata.py \
  --metadata-root 'raw_original/metadata/Layer Camera Metadata' \
  --output-dir processed/calibration/independent_metrology_metadata_audit_v1
```

| Before | After |
|---|---|
| DotGrid, SecondaryCamera Laser00, Checkerboard TIFF originals | read-only `tifffile.memmap(..., series=0, mode='r')`; no mutation |
| Calibration rank/H/config | no fit, rank selection, or config rewrite |
| Model/target/checkpoint/manufacturing A/B TIFF | not read or written |
| Output directory | artifact inventory CSV, summary JSON, one deterministic QC contact-sheet PNG only |

The audit reports the TIFF `series.axes`, shape, dtype, canonical channel×Y×X form, robust p01/p50/p99, gradient profile metrics, and—only for RGB secondary-camera data—a top-0.1%-red-dominance weighted centroid candidate. **Pass to later fit-audit consideration:** usable Y/X geometry for all artifacts plus reported laser candidate diagnostics. **Hold:** red candidate is not origin proof; dot/checkerboard texture metrics are not point/corner fits; do not alter `calibration_v1.yaml` or select rank from this pre-audit.
