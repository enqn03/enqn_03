# AMMT 프로젝트 실행 가이드 (Quickstart)

이 문서는 AMMT LPBF 공정 이상 후보 위치 탐지 모델의 **최신 아키텍처(v3 Regularized A+B Gated Fusion 모델)**를 훈련하고 평가하기 위한 핵심 실행 방법만을 다루는 퀵 매뉴얼입니다.

> 💡 **참고:** 프로젝트 초창기부터 진행해 온 방대한 데이터 검증(Audit) 스크립트들이나 과거 A-only, B-only 모델들의 실행 방법과 히스토리는 `python_scripts_execution_order.md` 문서를 참조하시기 바랍니다.

## 1. 사전 환경 및 데이터 준비

프로젝트 루트 디렉토리(`~/ammt_project/`)에서 모든 스크립트를 실행해야 하며, 데이터는 읽기 전용(`mode="r"`)으로 접근하는 것을 원칙으로 합니다.

### 필요 데이터 구조 (Read-only)
* **카메라 이미지:** `raw_original/layer_camera/LayerCameraAfterSpreading.tif` (A), `LayerCameraBurned.tif` (B)
* **시계열 분할 설정:** `manifests/causal_sequence_manifest.csv`
* **XCT 타겟 매핑 데이터:** `raw_original/registered_xct/`

모든 텐서 변환, 정규화(p01/p99), K=4 시계열 조립 및 타겟 매핑(Target Rasterization)은 `src/ammt_weak_target_dataset.py`에서 메모리 상에서(On-the-fly) 실시간으로 이루어지며, 디스크에 무거운 중간 산출물을 저장하지 않습니다.

## 2. 딥러닝 모델 훈련 실행

최신 과적합(Overfitting) 방지 정규화 기법인 Spatial Dropout과 Weight Decay(0.01)가 적용된 v3 모델을 학습하는 방법입니다.

```bash
cd ~/ammt_project

/usr/local/bin/python3 src/train_a_b_gated_fusion_v3.py \
  --config configs/a_b_gated_fusion_regularized_v3.yaml \
  --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif \
  --tiff-b raw_original/layer_camera/LayerCameraBurned.tif \
  --manifest manifests/causal_sequence_manifest.csv \
  --normalization-config configs/normalization_v1.yaml \
  --registered-root raw_original/registered_xct \
  --calibration-config configs/calibration_v1.yaml \
  --weak-target-config configs/weak_target_v1.yaml \
  --loss-config configs/masked_regression_loss_v1.yaml
```

* **Dry-run 테스트:** 만약 훈련을 시작하기 전에 입출력 텐서 형태나 에러 여부만 빠르게 점검하고 싶다면, 위 명령어 끝에 `--dry-run --dry-run-index 124` 옵션을 추가하세요.

## 3. 학습 결과 및 모델 평가 확인

훈련이 완료되면 설정 파일에 지정된 출력 경로(`outputs/a_b_gated_fusion_regularized_temporal_difference_v3/`)에 다음 파일들이 생성됩니다. 기존 폴더가 존재하면 덮어쓰기 에러(Fail-closed)가 발생하므로, 재실행 시 기존 폴더를 삭제하거나 이름을 변경해야 합니다.

| 생성 파일명 | 설명 | 활용 방법 |
|---|---|---|
| `best_validation_supported_loss.pt` | Validation Loss가 가장 낮았던 Epoch의 모델 가중치 파일 (Checkpont) | 이후 추론 스크립트에 넘겨 Test 평가 수행 |
| `training_history.json` | 에포크(Epoch)별 Train/Validation Loss 기록 | 학습 곡선(Learning Curve) 분석 |
| `test_metrics.json` | 최종 저장된 모델로 미지의 Test 셋을 평가한 결과 (Test Loss) | 모델 일반화 성능 측정 |
| `test_coordinate_candidates.json` | Test 이미지들에서 추출한 Top-5 이상 후보 위치와 점수들 | 실제 알람 및 사후 XCT 검증 좌표 |

### 후보 위치 상태(Status) 해석 가이드
추출된 `test_coordinate_candidates.json` 파일 내의 `status` 값을 확인하여 모델의 동작 상태를 판단해야 합니다.
* **`emitted`**: 정상적으로 이상 후보 좌표를 산출함 (가장 이상적임).
* **`withheld_top_score_plateau`**: 모델이 특정 노이즈를 식별하지 못해 결과 맵 전체를 동일한 최고점으로 도배해 버린 실패 상태 (좌표 추출 포기).
* **`withheld_temporally_invariant_map`**: 과거 레이어와 현재 레이어 결과 맵이 복붙한 듯이 완벽히 동일한 실패 상태.
