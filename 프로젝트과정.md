# AMMT Layer-camera 실시간 이상 후보 위치 탐지 프로젝트 과정

> **기록 원칙:** 이 문서는 프로젝트의 작업 목적, 변경 파일, 실행 결과, 판단, 다음 승인 필요 사항을 시간 순서로 기록한다. 원본 데이터는 수정하지 않으며, 코드·설정·결과 파일 변경은 사용자 승인 후에만 수행한다. 프로젝트 이력은 Git commit으로 함께 관리한다.

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트 목표 | LPBF 적층제조 layer-camera 영상에서 실시간 이상 후보 위치 `(x, y, layer_z, score)`를 출력하는 모델 구축 |
| 핵심 입력 | A: `LayerCameraAfterSpreading.tif`, B: `LayerCameraBurned.tif`, DAQ·XYPT·metadata |
| 핵심 방식 | A/B 공통 denoising backbone, A/B location heatmap, B 촬영 후 A/B fusion location heatmap |
| 현재 출력 원칙 | 결함 **확정**이 아닌 이상 후보 위치와 score를 출력 |
| 사후 품질 연결 | 등록판 XCT 품질 정보는 위치 검증·weak supervision 후보로 사용 |
| Git 원격 | `https://github.com/enqn03/enqn_03` |
| Linear | 사용 중단. Git과 본 문서로 과정 기록 유지 |

## 2. 데이터와 디렉터리 원칙

| 경로 | 역할 | Git 추적 |
|---|---|---|
| `raw_original/` | 원본 TIFF·공정·metadata·등록판 데이터. 변경 금지 | 제외 |
| `processed/` | 재생성 가능한 audit·전처리 결과 | 제외 |
| `src/` | 재현 가능한 전처리·학습 코드 | 추적 |
| `configs/` | ROI·정규화·축·분할 설정 | 추적 |
| `manifests/` | 원본을 가리키는 index와 split | 추적 |
| `output/` | 모델·시각화·평가 결과 | 제외 |
| `프로젝트과정.md` | 작업 과정·판단·승인·결과 기록 | 추적 |

## 3. 완료된 작업

### 3.1 원본 산업용 제조 데이터 수집 및 보존

| 시점 | 작업 | 결과 |
|---|---|---|
| 2026-08 | NIST AMMT Overhang Part X4 데이터 선정 | 의료 CT가 아닌 LPBF 제조 공정의 layer-camera·공정 시계열·사후 XCT 정합 자료를 확보 |
| 2026-08 | 원본 데이터 다운로드 및 보관 | `raw_original/layer_camera/`, `raw_original/process/`, `raw_original/metadata/`, `raw_original/registered_xct/` 구성 |
| 2026-08 | 원본 보존 규칙 확정 | 압축 해제 원본을 변경하지 않고, 모든 분석은 memmap 기반 읽기 전용으로 수행 |

### 3.2 프로젝트 Git 초기화

| 시점 | 작업 | 결과 |
|---|---|---|
| 2026-08-26 | Git 초기화 및 `.gitignore` 설정 | raw data·processed 결과·output은 Git에서 제외 |
| 2026-08-26 | GitHub 초기 업로드 | `main` 브랜치, 초기 commit `85c8d8e` 생성 |

### 3.3 A-only TIFF 구조 및 품질 audit

| 시점 | 작업 | 생성물 | 핵심 결과 |
|---|---|---|---|
| 2026-08-26 | `LayerCameraAfterSpreading.tif` read-only audit | `processed/audit_after_spreading/` | `TZYX=[3,250,2000,2000]`, `uint16`, 250 layer×LED 3방향 구조 확인 |
| 2026-08-26 | z=1~20·LED 1~3 통계·QC | `raw_inventory.csv`, `frame_stats.csv`, `audit_summary.json`, `qc_contact_sheet.png` | LED별 포화·밝기 차이가 커서 stage·LED별 정규화가 필요함을 확인 |

### 3.4 A/B pair audit

| 시점 | 작업 | 생성물 | 핵심 결과 |
|---|---|---|---|
| 2026-08-26 | A/B TIFF 대응 구조 검증 | `processed/audit_ab_pairs/ab_pair_audit_summary.json` | A/B 모두 `TZYX=[3,250,2000,2000]`, `uint16`으로 동일 `(z, LED)` pair 구성 가능 |
| 2026-08-26 | 초기·중기·후기 z 표본의 A/B 정량 비교 | `ab_pair_metrics.csv` | 표본 상관계수 약 0.75~0.87이 주로 관찰되어 큰 좌표 어긋남은 낮게 판단 |
| 2026-08-26 | A/B/`abs(B-A)` QC 시각화 | `ab_pair_contact_sheet.png` | raw 차이에는 정상 용융 변화·고정 설비·반사 변화가 함께 포함되므로 결함 label로 직접 사용하면 안 됨 |

## 4. 현재 승인된 작업: ROI·saturation audit

### 목적

A/B fusion 전에 모델이 학습해야 할 공정 관심 영역(ROI)을 확인하고, camera 테두리·고정 설비·지속 포화 영역의 영향을 측정한다. 이 결과로 이후의 crop 및 stage·LED별 normalization 설정을 결정한다.

### 승인된 변경 범위

| 추가 대상 | 목적 | 원본 영향 |
|---|---|---|
| `src/audit_roi_saturation.py` | 후보 ROI 내 A/B 밝기·포화·차이 통계와 QC 생성 | 원본 TIFF 읽기 전용 |
| `processed/roi_audit/roi_saturation_summary.csv` | 후보 ROI의 sample 통계 | 소형 재생성 결과 |
| `processed/roi_audit/roi_candidate_qc.png` | ROI와 A/B saturation frequency 시각화 | 소형 재생성 결과 |

### 실행 원칙

* A/B TIFF는 `tifffile.memmap(..., mode="r")`으로만 연다.
* 초기 후보 ROI는 `(x0=250, y0=250, x1=1750, y1=1750)`로 평가하며, 이는 최종 확정값이 아니다.
* ROI audit 결과를 검토한 뒤에만 `configs/roi.yaml`을 새로 만들 수 있다.

## 5. 다음 승인 필요 작업

| 우선순위 | 작업 | 예상 추가 파일 | 승인 필요 이유 |
|---:|---|---|---|
| 1 | ROI audit 결과 검토 및 ROI 확정 | `configs/roi.yaml` | 모델 입력 영역을 결정하는 설정 변경 |
| 2 | training-only normalization parameter 산출 | `configs/normalization.yaml`, `processed/normalization/` | train/validation 분할과 정규화 기준 확정 |
| 3 | causal sequence manifest 생성 | `manifests/causal_sequences.csv`, `manifests/split_manifest.csv` | 학습 샘플과 시간 누수 방지 분할 정의 |
| 4 | A-only location baseline 구현 | `src/datasets/`, `src/models/`, `src/train_*.py` | 학습 코드와 모델 파일 추가 |

## 6. 향후 기록 양식

모든 이후 작업은 아래 형식으로 이 문서 끝에 추가한다.

```markdown
### YYYY-MM-DD — 작업명

| 항목 | 내용 |
|---|---|
| 목적 | |
| 승인 범위 | |
| 변경 파일 | |
| 입력 데이터 | |
| 실행 명령 | |
| 결과 | |
| 판단 | |
| Git commit | |
| 다음 승인 필요 작업 | |
```

## 7. 참고 자료

[1] NIST, *Process Monitoring Dataset from the Additive Manufacturing Metrology Testbed (AMMT): Overhang Part X4*. https://data.nist.gov/od/id/mds2-2233

### 2026-08-26 — ROI·saturation audit 실행

| 항목 | 내용 |
|---|---|
| 목적 | A/B fusion 전 후보 ROI 안의 포화·밝기·A/B 차이를 확인하고, 최종 model ROI를 결정할 근거를 확보 |
| 승인 범위 | `src/audit_roi_saturation.py` 추가 및 `processed/roi_audit/`의 소형 CSV·PNG 생성 |
| 입력 데이터 | `LayerCameraAfterSpreading.tif`, `LayerCameraBurned.tif` (모두 `memmap(mode="r")` 읽기 전용) |
| 실행 명령 | `/usr/local/bin/python3 src/audit_roi_saturation.py --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif --tiff-b raw_original/layer_camera/LayerCameraBurned.tif --output-dir processed/roi_audit --candidate-roi 250 250 1750 1750 --z-values 1 10 20 125 230 250 --reference-z 125 --reference-led 1` |
| 생성 결과 | `processed/roi_audit/roi_saturation_summary.csv`, `processed/roi_audit/roi_candidate_qc.png` |
| 원본 영향 | 원본 TIFF·기존 audit 결과·metadata는 수정·이동·삭제하지 않음 |
| 결과 | A/B 구조는 정상적으로 읽혔으나, 후보 ROI 내부에도 LED 1·2의 full-scale saturation이 넓게 남아 있음. 특히 z=125에서 A LED 1 약 27.7%, B LED 1 약 42.3%, A LED 2 약 42.5%, B LED 2 약 43.0%의 포화가 관찰됨. |
| 판단 | `(250,250)-(1750,1750)`은 외곽 고정 설비는 일부 제외하지만 **최종 model ROI로 확정하지 않음**. 단일 crop만으로는 부족하며, stage·LED별 normalization과 학습 시 사용할 saturation validity mask 정책이 필요함. |
| 다음 승인 필요 작업 | training 구간에서 ROI 후보·pixel saturation frequency를 확장 측정하고, `configs/roi.yaml` 및 `configs/normalization.yaml` 초안을 생성하는 작업 |

## 8. Git 기록

* 2026-08-26: 초기 기록 commit `85c8d8e`와 ROI·saturation audit·프로젝트 과정 commit `daa77a3`를 GitHub 원격 `main`에 반영함.
* 2026-08-26: GitHub가 기존 `enqn03/enqn03` 주소의 `enqn03/enqn_03` 이전을 안내했으며, 이후 Git 작업부터 `origin` URL을 새 주소로 갱신함.
* Git은 코드·설정·manifest·`프로젝트과정.md`만 추적하며, 원본 TIFF와 `processed/` 산출물은 추적하지 않는다.


### 2026-08-26 — ROI 후보 비교 audit 실행

| 항목 | 내용 |
|---|---|
| 목적 | 단일 후보 ROI `(250,250)-(1750,1750)`를 확정하지 않고, 중첩·수직 이동 ROI 후보의 sampled-frame full-scale saturation과 유효 픽셀 비율을 비교 |
| 승인 범위 | `src/audit_roi_candidates.py` 추가, `processed/roi_candidates/` 아래의 소형 CSV·JSON·PNG audit 결과 생성, Git 기록 |
| 변경 파일 | `src/audit_roi_candidates.py`, `프로젝트과정.md` 및 Git 설정의 원격 URL(승인 후 갱신 예정) |
| 입력 데이터 | `raw_original/layer_camera/LayerCameraAfterSpreading.tif`, `raw_original/layer_camera/LayerCameraBurned.tif`; `TZYX=[3,250,2000,2000]`, `uint16` |
| 실행 명령 | `/usr/local/bin/python3 src/audit_roi_candidates.py --tiff-a raw_original/layer_camera/LayerCameraAfterSpreading.tif --tiff-b raw_original/layer_camera/LayerCameraBurned.tif --output-dir processed/roi_candidates` |
| 생성 결과 | `processed/roi_candidates/roi_candidate_comparison.csv`, `roi_candidate_aggregate.csv`, `roi_candidate_summary.json`, `roi_candidate_qc.png` |
| 원본 영향 | `tifffile.memmap(..., mode="r")`으로만 열었으며 원본 TIFF·기존 audit 결과·metadata를 수정·이동·삭제하지 않음 |
| 검토 범위 | z=`[1,10,20,125,230,250]`과 LED=`[1,2,3]`의 A/B 대응 18 frame pair를 대상으로 5개 후보 ROI 비교 |
| 결과 | 평균 full-scale saturation은 wide `(250,250)-(1750,1750)` 34.53%, lower `(350,450)-(1650,1750)` 35.56%, inner `(350,350)-(1650,1650)` 38.78%, upper `(350,250)-(1650,1550)` 40.21%, inner `(450,450)-(1550,1550)` 40.52%였다. 모든 후보에서 worst sampled frame saturation은 91.28%~99.10%였다. |
| LED·stage 관찰 | wide 후보에서 A LED1/LED2 평균 saturation은 각각 35.62%/51.19%, B LED1/LED2는 45.44%/49.94%인 반면 LED3는 A 11.90%, B 13.11%였다. 후보를 중심으로 좁힐수록 saturation이 낮아지지 않았으며, LED1/2와 B 단계의 포화 영향이 특히 컸다. |
| QC 해석 | A/B LED1 saturation frequency map에서 지속 포화 중심 영역과 상단 띠가 보이며, 모든 비교 ROI가 이 영역을 상당 부분 포함한다. 따라서 crop만으로 포화를 제거하려는 정책은 부적절하다. |
| 판단 | saturation 수치만으로 최종 ROI를 선택하지 않는다. 현 시점에서는 기존 wide 후보가 상대적으로 가장 낮은 평균 포화를 보존하지만, **최종 ROI는 미확정**이며 A/B·LED별 saturation validity mask와 training-only normalization이 필수다. |
| Git commit | 기록 및 원격 push 완료 후 commit hash를 갱신 |
| 다음 승인 필요 작업 | (1) ROI 공간 범위·공정 관찰 보존 조건을 포함한 `configs/roi_v1.yaml` 검토·확정, (2) train 구간만 사용한 A/B·LED별 robust normalization 및 saturation validity-mask config 초안 생성 |


### 2026-08-26 — 로컬 Git 동기화 및 사용자 실행용 causal manifest 코드 제공

| 항목 | 내용 |
|---|---|
| 목적 | 원격 GitHub `main`의 ROI 후보 audit 기록과 로컬 프로젝트를 동기화하고, 미래 layer 누수 없이 causal sequence index를 만들 코드만 제공 |
| 승인 범위 | 로컬 `origin/main` 동기화, `src/build_causal_manifest.py` 추가, 본 문서와 Git 기록. Python 실행은 사용자가 직접 수행 |
| 사용자 실행 원칙 | 이후 Python 파일은 에이전트가 실행하지 않는다. 코드 위치·명령·입력·출력·실행 전후 상태를 먼저 제공하고 사용자가 직접 실행한다. |
| 로컬 Git 동기화 | `git fetch origin && git reset --hard origin/main` 실행 후 local `main`과 `origin/main`이 commit `bdb7e98`로 동기화됨. 이 reset은 사용자 승인 범위이며, 원격과 동일한 `프로젝트과정.md`·ROI audit 코드만 보존함. |
| 추가 코드 | `src/build_causal_manifest.py` |
| 코드 역할 | TIFF를 열지 않고 layer index만으로 인과적 sequence manifest와 split policy JSON을 생성. 원본 TIFF 픽셀·crop·정규화·label은 읽거나 생성하지 않음. |
| 기본 시간 정책 | K=4, train endpoint z=4–150(147개), guard z=151–153, validation endpoint z=154–200(47개), guard z=201–203, test endpoint z=204–250(47개), 총 241개 sample index |
| 누수 방지 규칙 | 각 history는 `[z-3,z-2,z-1,z]`이며 미래 layer를 사용하지 않는다. validation/test는 각각 직전 guard layer와 자기 split endpoint만 history로 사용하며 다른 split의 endpoint layer를 사용하지 않는다. |
| 사용자 실행 명령 | `/usr/local/bin/python3 src/build_causal_manifest.py --output-csv manifests/causal_sequence_manifest.csv --policy-json manifests/split_policy.json` |
| 실행 전 상태 | `manifests/` 아래 causal CSV·policy JSON이 없음. raw TIFF와 `processed/` 내용은 변화 없음. |
| 실행 후 예상 상태 | `manifests/causal_sequence_manifest.csv`와 `manifests/split_policy.json`이 새로 생성됨. 둘은 image data가 아닌 작은 text index이며 Git 추적 대상임. |
| 에이전트 실행 여부 | 사용자 요청에 따라 Python 코드는 실행하지 않음. 따라서 manifest 출력값은 사용자가 명령을 실행한 뒤에만 검토 가능. |
| Git commit | 코드·기록 commit 및 push 후 hash를 아래 Git 이력으로 확인 |
| 다음 승인 필요 작업 | 사용자가 manifest 실행 결과를 공유한 뒤 train endpoint만 사용한 stage/LED normalization 통계와 saturation validity-mask 설정 초안 제안 |


### 2026-08-26 — causal manifest 검증 버그 수정 및 64:16:20 endpoint split 적용

| 항목 | 내용 |
|---|---|
| 발생 오류 | 기존 `src/build_causal_manifest.py`가 train의 첫 endpoint z=4 history `[1,2,3,4]`에서 정상적인 초기 history z=1–3을 다른 split 누수로 잘못 판정하여 manifest 생성이 중단됨 |
| 승인 범위 | `src/build_causal_manifest.py`의 history 허용 검증과 기본 split 변경, 본 문서와 Git 기록. Python 실행은 사용자가 직접 수행 |
| 버그 수정 | train split은 첫 endpoint 이전의 필요한 warm-up history를 허용한다. K=4·train 시작 z=4에서는 z=1–3이 허용되며, validation/test는 직전 3-layer guard와 자기 endpoint 범위만 허용한다. |
| 승인 비율 | train:validation:test=`6.4:1.6:2.0` = `64%:16%:20%` |
| endpoint 배분 원칙 | K=4의 첫 3개 layer와 두 guard band(각 3개)를 고려하면 사용 가능한 endpoint는 241개이다. 241×비율의 가장 가까운 정수 배분은 train 154개, validation 39개, test 48개로 합계 241개이다. |
| 갱신된 기본 정책 | train endpoint z=4–157 (154개), guard z=158–160, validation endpoint z=161–199 (39개), guard z=200–202, test endpoint z=203–250 (48개) |
| 실제 endpoint 비율 | train 63.90%, validation 16.18%, test 19.92%로, endpoint 정수 제약 아래 승인 비율에 가장 가깝다. |
| 재실행 명령 | `/usr/local/bin/python3 src/build_causal_manifest.py --output-csv manifests/causal_sequence_manifest.csv --policy-json manifests/split_policy.json` |
| 실행 전 상태 | 이전 실행은 validation 전에 오류로 종료되었고 manifest output 파일은 생성되지 않음. raw TIFF·processed data는 변화 없음. |
| 에이전트 실행 여부 | 사용자 요청에 따라 수정 후 Python 파일을 실행하지 않음. 사용자가 직접 실행한 출력으로만 결과를 검토한다. |
| Git commit | 코드·기록 commit 및 push 후 Git 이력에서 확인 |
| 다음 승인 필요 작업 | 사용자의 정상 manifest 실행 결과 확인 후, train endpoint만 사용한 stage/LED normalization 통계 및 saturation validity mask 정책 설계 |

